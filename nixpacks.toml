[phases.setup]
nixPkgs = ["python312", "nodejs-18_yarn"]

[phases.build]
cmds = ["cd backend && pip install -r requirements.txt", "cd frontend && npm install", "cd frontend && npm run build"]

[start]
cmd = "python backend/main.py"

[static]
# Frontend статика будет обслуживаться через API или отдельный сервис
