[phases.setup]
nixPkgs = ["nodejs-18_x", "yarn", "python312", "python312Packages.pip"]

[phases.build]
cmds = ["cd backend && python3 -m pip install -r requirements.txt", "cd frontend && npm install", "cd frontend && npm run build"]

[start]
cmd = "python backend/main.py"

[static]
# Frontend статика будет обслуживаться через API или отдельный сервис
